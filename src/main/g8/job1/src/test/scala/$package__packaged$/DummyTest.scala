// $copyright
package $organization$.$name$

import org.scalatest.{ FlatSpec, Matchers }

/**
 * A simple test for business logic without spark.
 */

class DummyTest extends FlatSpec with Matchers {
  "A dummy test" should "be correct" in {
    assert(true, true)
  }
}
